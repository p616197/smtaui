_schema-version: "3.2"
ID: smtaui
version: 0.0.1
modules:
- name: smtaui-approuter
  type: approuter.nodejs
  path: smtaui-approuter
  requires:
  - name: smtaui_html_repo_runtime
  parameters:
    disk-quota: 256M
    memory: 256M
- name: smtaui_ui_deployer
  type: com.sap.application.content
  path: .
  requires:
  - name: smtaui_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - gws-content.zip
      name: gws
      target-path: resources/
- name: gws
  type: html5
  path: gws
  build-parameters:
    builder: custom
    commands:
    - npm run build
    supported-platforms: []
resources:
- name: smtaui_html_repo_runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
- name: smtaui_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-host
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm install
